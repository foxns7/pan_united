package challenge2;

import java.util.ArrayList;



public class challenge2 { // inner class
	private class SumManager {

		ArrayList<ArrayList<Integer>> listOfGroups = new ArrayList<>();
		ArrayList<Integer> numbers = new ArrayList<>();
		int sum; 
		
		public SumManager() {
			listOfGroups.add(new ArrayList<>());
		}
		
		public void add(int number) {
			listOfGroups.get
		}
		
		public int groups() {
			return 
		}
		
	}
	static int lol(int[] data) {
		SumManager manager = new SumManager();
		
		for(int number: data) {
			manager.add(number);
		}
		return listOfSums.size();
	}

	public static void main(String[] args) {
		int[] data = {1,4,1,3,1};
		lol(data);
	}

}
